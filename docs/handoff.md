# Aster 引き継ぎ書(2026-07-31 時点・第3版)

このセッションでの作業のまとめです。次にこのプロジェクトを見るAI(Claude/ChatGPT問わず)は、まずこのファイル全体を読んでから着手してください。

---

## 1. プロジェクト概要

「Aster」は、Discord上で動作する人格付きAIアシスタント。単なる質問応答Botではなく、「AIアシスタント」ではなく対等な友達(少しだけ恋愛要素あり)として自然に会話することを目指している。文章生成はGemini API。

- GitHub: https://github.com/yuki967787/Aster_2 (Public)
- 開発環境: Windows / VS Code / Python venv(`.venv`)
- 実行方法: `set PYTHONPATH=src && python -m aster.main` (Windows)
- 将来的な目標: このBotを友達に配布したいと考えている。配布先のユーザーごとに好きな性格へカスタマイズできるようにしたい、という構想がある(現時点では未着手)。

---

## 2. 直近の進捗・対応内容 (2026-07-31)

- **起動エラーの修正**: `src/aster/handwriting.py` におけるフォントロード周りの `FileNotFoundError` を修正。
  - `Path(__file__).resolve().parent / "assets" / "fonts"` を基準とした確実なパス解決に変更。
  - `pyos.otf`（本命）および `Yomogi-Regular.ttf`（フォールバック）の柔軟な自動検出ロジックを導入。
  - 正しいフォルダ構成（`src/aster/assets/fonts/`）にフォントを配置することで、正常起動を確認済み。

---

## 3. このセッションで決まった仕様・決定事項(重要)

- `/chat` のようなスラッシュコマンドは無し。メンションされたら返信する方式(`on_message`)のみ。
- 返信はDiscordの引用返信(reply)を使わず、通常送信(send)で統一。
- 短期記憶はチャンネル単位・直近20件まで。長期記憶は自由記述 `notes` 1本、5分デバウンスで自動抽出。
- 添付画像対応: 1メッセージ最大6枚。画像内容はGeminiに120字程度で説明させたテキストとして短期記憶に残す。
- モデル振り分け: 通常会話・画像系はメインモデル(`GEMINI_MODEL_MAIN`)、長期記憶抽出は軽量モデル(`GEMINI_MODEL_LIGHT`)。
  - 2026年7月31日時点でのデフォルトは `gemini-3.6-flash` / `gemini-3.5-flash-lite`。404エラーが出たら公式ドキュメントで現行モデル名を確認すること。
- persona.txt: 「少し恋愛要素のある対等な友達」に調整。「笑」は1返信に最大1つ。
- 絵文字リアクション: 返答末尾の `[EMOJI: 🎉]` タグから抽出して `add_reaction()`。
- 手書きノート画像: 返答末尾の `[MODE: NOTE]` タグを検出し、`handwriting.py` でノート画像化して送信。
- **コード修正のルール**: どんな変更も「変更前後の理由と影響範囲」を説明してから実施する(次のAIも踏襲すること)。

---

## 4. 保留中・未着手の課題 (次にやること)

- **実機テストの通し確認**: 起動確認は完了したため、実際にDiscord上でスタンプ(絵文字リアクション)、手書きノート画像生成、モデル振り分け動作のテストを行う。
- **配布・性格カスタマイズ構想**: `persona.txt` のマルチユーザー対応(サーバー/ユーザーごとの設定切り替え等)。大きな変更となるためユーザー(ゆうき)と事前相談必須。
- Phase 2-5の残り(リアクション・Embed)、Phase 3(PDF解析/OCR)、Phase 4(VOICEVOX)。

---

## 5. 動作確認コマンド

```bash
# 起動(Windows)
set PYTHONPATH=src && python -m aster.main